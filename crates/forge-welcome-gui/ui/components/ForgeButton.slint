export component ForgeButton inherits Rectangle {
    in property <string> icon;
    in property <string> label;
    in property <bool> primary: false;
    in property <bool> enabled: true;
    in property <length> horizontal-padding: 26px;
    in property <length> label-size: 20px;
    in property <length> icon-size: 30px;

    callback clicked;

    width: 280px;
    height: 80px;

    border-radius: 8px;
    border-width: 2px;
    border-color: !enabled ? #555555 : primary ? #ff7a18 : #777777;
    background: !enabled ? #18181888 : primary ? #18120fdd : #181818cc;

    TouchArea {
        enabled: root.enabled;
        clicked => {
            root.clicked();
        }
    }

   if root.icon != "": HorizontalLayout {
        padding-left: root.horizontal-padding;
        padding-right: root.horizontal-padding;
        spacing: 14px;

        Text {
            text: root.icon;
            color: !root.enabled ? #777777 : root.primary ? #ff7a18 : #ffffff;
            font-size: root.icon-size;
            vertical-alignment: center;
        }

        Text {
            text: root.label;
            color: !root.enabled ? #777777 : root.primary ? #ff7a18 : #ffffff;
            font-size: root.label-size;
            font-weight: 700;
            vertical-alignment: center;
        }
    }

    if root.icon == "": Text {
        text: root.label;
        color: !root.enabled ? #777777 : root.primary ? #ff7a18 : #ffffff;
        font-size: root.label-size;
        font-weight: 700;

        width: parent.width;
        height: parent.height;

        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
